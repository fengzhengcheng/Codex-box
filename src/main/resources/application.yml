server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/weather_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.bingran.entity
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
