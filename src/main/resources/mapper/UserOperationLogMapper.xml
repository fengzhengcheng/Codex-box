<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bingran.mapper.UserOperationLogMapper">
    <resultMap id="BaseResultMap" type="com.bingran.entity.UserOperationLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="operation_content" property="operationContent"/>
        <result column="operation_ip" property="operationIp"/>
        <result column="operation_time" property="operationTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, user_id, operation_content, operation_ip, operation_time
        FROM user_operation_log
        ORDER BY operation_time DESC
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, operation_content, operation_ip, operation_time
        FROM user_operation_log
        WHERE user_id = #{userId}
        ORDER BY operation_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_operation_log (user_id, operation_content, operation_ip)
        VALUES (#{userId}, #{operationContent}, #{operationIp})
    </insert>
</mapper>
